# SafeCart PWA

Mobile-first progressive web app for SafeCart. Implementation starts only after the required SafeCart AI checks pass.

The PWA calls `SafeCart-API`; it never calls `SafeCart-AI` or `SafeCart-ScrapingData` directly.
