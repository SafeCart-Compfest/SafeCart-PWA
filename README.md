# SafeCart PWA

Mobile-first progressive web app for SafeCart. Implementation starts only after the SafeCart AI acceptance gate passes.

The PWA calls `SafeCart-API`; it never calls `SafeCart-AI` or `SafeCart-ScrapingData` directly.